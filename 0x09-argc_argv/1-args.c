#include <stdio.h>
/**
 * main - Entry point
 *@argc: Argument
 *@argv: pointer to a string
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{

	printf("%d\n", argc - 1);
	(void)argv;

	return (0);
}
